#include "../expr.h"

struct val list_prim(struct val_list *list);
struct val length_prim(struct val_list *list);
struct val map_prim(struct val_list *list);
struct val apply_prim(struct val_list *list);
